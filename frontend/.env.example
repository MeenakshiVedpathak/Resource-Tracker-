# API base url is runtime config, not a build-time env var — see public/env-config.js
# and ENVIRONMENTS.md. The Microsoft redirect uri is derived from window.location.origin.

VITE_APP_NAME=Trackio
VITE_APP_VERSION=1.0.0

